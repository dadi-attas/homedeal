/* ============================================================
   HOMEDEAL — script.js (לוגיקת העמוד הציבורי)
   ------------------------------------------------------------
   ייבנה בשלבים 1–4:
   שלב 1 — רינדור הירו, כרטיסי מידע, פרטי נכס, תנאים, דיסקליימר
   שלב 2 — גלריית מדיה מובנית + לייטבוקס + סרטון
   שלב 3 — שאלון + מנוע ניקוד + שאלות סף
   שלב 4 — פלואו יצירת קשר + WhatsApp
   ------------------------------------------------------------
   כל התוכן נטען מ-HOMEDEAL_CONFIG (config.js). אין לקבע נתונים כאן.
   שום נתון של מבקר אינו נשמר או נשלח.
   ============================================================ */

(function () {
  "use strict";

  // בדיקת קיום קונפיג — יוחלף ברינדור מלא בשלב 1
  if (typeof HOMEDEAL_CONFIG === "undefined") {
    console.error("HOMEDEAL: config.js לא נטען");
    return;
  }

  console.log("HOMEDEAL config version:", HOMEDEAL_CONFIG.meta.configVersion);
})();
